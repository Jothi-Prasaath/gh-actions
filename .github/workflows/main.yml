name: First-Workflow
on: workflow_dispatch
jobs:
  First-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "hello world!"
      - name: Print Bye
        run: echo "bye"
